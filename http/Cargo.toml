[package]
name = "http"
version = "0.1.0"
edition = "2024"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[lib]
crate-type = [ "cdylib" ]

[dependencies]
bstr = "1.12.1"

[dev-dependencies]
serde = { version = "1.0.228", features = ["serde_derive"] }
serde_json = "1.0.149"
